import React from 'react';
import './global.css';
import './LoadingView.global.css';

const LoadingView = () => {
  return <div className="loadingContainer">Loading ...</div>;
};

export default LoadingView;
